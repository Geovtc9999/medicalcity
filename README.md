# medicalcity

This repo is wired with [Graft](https://github.com/NanoNets/Graft) so Cursor can query a local context graph of the code.

After cloning, generate the graph (no API key required):

```bash
npx @nanonets/graft build
```

